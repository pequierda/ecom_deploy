
DB_HOST=0.tcp.ap.ngrok.io        # from `ngrok tcp 3306` output
DB_PORT=12345                    # from the same output
DB_USER=emilpro                  # your MySQL user
DB_PASSWORD=3m1lpr0123           # your MySQL password
DB_NAME=wedding_mart_db
DB_SSL=0                         # ngrok TCP is plain by default

# API server config
ALLOWED_ORIGINS=http://localhost:5173,https://ecom-deploy-one.vercel.app/
UPLOADS_ROOT=uploads             # leave as-is unless you change storage
SERVE_UPLOADS_STATIC=true